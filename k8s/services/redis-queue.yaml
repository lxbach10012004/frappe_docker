apiVersion: v1
kind: Service
metadata:
  name: redis-queue
  namespace: erpnext
spec:
  ports:
  - port: 6379
    targetPort: 6379
  selector:
    app: redis-queue
  clusterIP: None # Headless service for StatefulSet